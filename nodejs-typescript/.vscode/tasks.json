{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "compile",
      "type": "shell",
      "command": "npm run compile",
      "problemMatcher": "$tsc",
      "dependsOn": "npm: install - ${workspaceFolder}"
    }
  ]
}
